<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('../partials/head', { title: 'Iniciar sesión | TechStore' }) %>
</head>
<body>
    <%- include('../partials/header') %>

    <main class="auth-container">
        <section class="auth-card login-card">
            <header class="auth-header">
                <div class="auth-logo">TechStore</div>
                <div class="auth-decoration"></div>
            </header>

            <div class="auth-content">
                <h1 class="auth-title">Iniciar sesión</h1>
                <p class="auth-subtitle">La mejor opción en Tecnología</p>
                
                <!-- Mensaje de registro exitoso -->
                <% if (typeof registered !== 'undefined' && registered === 'true') { %>
                    <div class="alert alert-success">
                        <p>¡Registro exitoso! Ahora puedes iniciar sesión.</p>
                    </div>
                <% } %>
                
                <!-- Mensaje de error (en caso de que el controlador lo envíe) -->
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger">
                        <p><%= error %></p>
                    </div>
                <% } %>

                <!-- 
                  ====================================================================
                  == CAMBIO CLAVE: La acción ahora apunta a /auth/login para que     ==
                  == nuestro controlador de autenticación procese el formulario.    ==
                  ====================================================================
                -->
                <form class="auth-form" action="/auth/login" method="POST">
                    <div class="form-group">
                        <label for="correo">Correo electrónico</label>
                        <input type="email" id="correo" name="correo" placeholder="tucorreo@ejemplo.com" required>
                    </div>

                    <div class="form-group">
                        <label for="password">Contraseña</label>
                        <input type="password" id="password" name="password" placeholder="Ingresa tu contraseña" required>
                    </div>

                    <div class="form-options">
                        <label class="checkbox-label">
                            <input type="checkbox" name="remember">
                            <span class="checkmark"></span>
                            Recordarme
                        </label>
                        <a href="#" class="link">¿Olvidaste tu contraseña?</a>
                    </div>

                    <button type="submit" class="btn btn-primary btn-full">Iniciar sesión</button>
                </form>

                <div class="auth-divider">
                    <span>O</span>
                </div>

                <div class="social-auth">
                    <button type="button" class="btn btn-social btn-google">
                        <span class="social-icon">G</span>
                        Continuar con Google
                    </button>
                </div>

                <footer class="auth-footer">
                    <p>¿No tienes una cuenta? <a href="/register" class="link">Regístrate</a></p>
                </footer>
            </div>
        </section>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>